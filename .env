# General (you can keep these)
APP_NAME=Frequency
ENV=dev
PORT=8000

# --- Model ---
# Expected fraud probability in benign conditions (0<p<1)
BASE_FRAUD_RATE=0.02
BUILD_WEIGHTS=false   # true = retrain, false = load saved

# Path to save/load raw probabilities
RAW_PROBS_FILE=src/freq_app/data/raw_probs.json

# Port for the FastAPI server
PORT=8000
